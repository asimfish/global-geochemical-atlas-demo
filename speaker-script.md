# 全球地球化学元素分布图谱 Skill：三分钟最终演示讲稿

总时长 180 秒，共 12 页。主线：赛题拆解 → 五项交付逐项演示（每页配对应界面实况）→ Skill 内核状态机 → 质量控制 → 最终效果 → 运行实况 → 收尾。若临场超时，第 8、9 页可各压缩到一句话。

## 0:00–0:12｜第 1 页：不是地图，而是科研工作流

> 我们交付的不是一张预先做好的地图，而是一套可执行科研工作流。右边是它的真实运行界面，仓库地址就在屏幕上，代码和 Skill 文档全部开源可查。

动作：按 `T` 启动计时。指右侧浏览器窗口与仓库链接。

## 0:12–0:32｜第 2 页：赛题要求，逐条落地

> 赛题要求三件事：多介质自动采集、科学标准化、研究出图与异常识别。我们已接入十五个公开来源，覆盖六大洲与全球淡水——欧洲 FOREGS 六介质和 GEMAS、非洲 AfSIS、联合国 GEMStat 全球淡水、GEOROC 的南极与太古宙克拉通、日本 GSJ、澳大利亚 NGSA、北非 PANGAEA、中国 TPDC。同一次 Skill 运行，同时产出五项交付，下面逐项看。

动作：扫过三行任务拆解，点一下来源条，落在五项交付。

## 0:32–0:54｜第 3 页：交付 1 · 可交互元素分布地图

> 第一项：可交互元素分布地图。三步上手：选条件、看分布、点记录——热力是采样密度，空白区域如实显示数据缺口，任意点位直达原始证据。右边是同一份数据的三维地球仪，可拖动旋转。数据在线采集成库，产物是自包含单文件，无需服务器与外网，评审现场断网照样全功能交互。

动作：指三步用法，再指 3D 地球仪；时间充裕点“启用交互”缩放一次。

## 0:54–1:12｜第 4 页：交付 2 · 标准化地球化学数据库

> 第二项：标准化数据库，一张四十多个字段的专业长表，一行就是一个样品的一项元素测定。record_id 全库唯一、样品带 IGSN；原始值和标准化值双列并存，换算因子单独成列；删失值保留检出限 qualifier、不插补为零；坐标统一 WGS84 带不确定度，地质单元来自 GLiM。语义对齐 ODM2，字段映射对照 EarthChem 和 USGS。下载的 CSV 就是交付物本体。

动作：指右上角字段字典，再指“下载完整 geochemistry.csv”。

## 1:12–1:28｜第 5 页：交付 3 · 数据来源与置信度说明

> 第三项：来源与置信度。五维加权，全部是真实运行值：来源可靠性 0.897、字段完整性 0.956、方法可比性 0.813、空间精度 0.800、QC 质量 0.876，综合 0.874；全库八成高置信。每条记录沿证据链能追到原始源行和 SHA-256。我们如实标注：置信度衡量工作流可用性，不等于测量正确概率。

动作：指五根指标条，再指右上角分级卡。

## 1:28–1:48｜第 6 页：交付 4 · 异常区域识别结果

> 第四项：异常识别，主图就是可视化的异常区域——红环富集候选、蓝环亏损候选，环上数字是候选数。右侧缩略图打开候选列表和判据卡片：每个候选给出网格、方向、元素、最大 z 值，还解释“为什么被标记”，比如比背景中位数高多少倍。判定用 robust z 不小于 3.5，背景组限定同元素、同介质、同测量基准。候选不等于矿化定论。

动作：指地图上的红蓝环，再指右侧缩略图。

## 1:48–2:04｜第 7 页：交付 5 · 可复用 Skill 文档

> 第五项、也是最终交付物：可复用 Skill 文档，四层结构——SKILL.md 状态机与决策门、references 科学知识库按需加载、scripts 确定性执行零第三方依赖、fixtures 加测试证明可复现可评测。Agent Skills 公共子集格式，OpenCode 评测已接入，Codex、CLI、CI 直接调用。

动作：从 L1 扫到 L4。

## 2:04–2:20｜第 8 页：Skill 内核，状态机 × iteration loop

> 这是它区别于“长提示词”的内核：前向是九阶段确定性状态机，三道决策门不满足就停；反向是核心机制——可审计 iteration loop：验证十五项产物、问题登记进 backlog、按问题码定向修复、重跑比较，最多两轮，不降即停转人工。八个稳定失败状态码，Agent 和 CI 都能编程处理。

动作：左手扫九宫格状态机，右手指旋转的 loop 环。

## 2:20–2:34｜第 9 页：质量控制，真实迭代清单

> 质量控制的证据就在产物里：本次运行的 iteration_backlog 显式登记了 114,807 项问题，零静默删除。屏幕上是真实片段——方法范围未声明、标准化失败、坐标不可用、删失观测，每项都有稳定编号和处置规则；八万九千项自动处置、两万五千项人工复核、二百四十八项科学限制不计失败。

动作：指真实表格两行，再指底部三类状态计数。

## 2:34–2:42｜第 10 页：最终效果

> 这些不是设计图，是真实运行产物：全球图谱与区域研究视图，共享同一数据库和证据链，可以现场打开操作。

动作：可点击“打开交互图谱”缩放一次（控制在 6 秒内）。

## 2:42–2:54｜第 11 页：运行实况，Qwen3.8-Max 用 Skill 干活

> 最后看通用 Agent 用我们 Skill 干活的实况：Qwen3.8-Max 在 OpenCode 挂载 Skill，自己读 SKILL.md、走九阶段——来源过审、带证据采集、标准化、地质匹配、置信度与异常、产物验证，右侧交互图谱、数据库、异常结果实时就绪。模型只做编排与解读，科学计算全由确定性脚本完成；有无 Skill 对照评测，提升是实测的。

动作：让日志动画自己跑完（约 7 秒），指右侧陆续点亮的产出卡。

## 2:54–3:00｜第 12 页：收尾

> 这个 Skill 叫 Global Geochemical Atlas。五项输出来自它的一次执行；最终提交物只有一个：完整、可复用的 Skill 文档。谢谢。

动作：停在中心 logo 与 “GLOBAL GEOCHEMICAL ATLAS”。

## 现场控制

- `←` / `→`、`PageUp` / `PageDown`、空格：翻页。
- `N`：显示或隐藏当前页讲稿；`T`：计时；`R`：归零；`F`：全屏。
- 第 3–6 页可点“启用交互”在当前页操作对应视图（首次点击约一两秒加载）；“新窗口打开”直达完整页面。
- 第 3 页地球仪小窗可点击放大到新窗口；第 11 页重新进入可重放动画。

## 必须保持的口径边界

- Agent 平台方面：表述为“Agent Skills 公共子集；OpenCode 评测与挂载已接入；Codex/CLI/CI 可通过普通接口调用”，不说所有平台都通过官方认证。
- 专业平台方面：EarthChem ECL、USGS AGDB2、ODM2、IGSN/DataCite 是字段语义映射对照，不表述为平台认证或原生格式导出。
- 来源定位与哈希证明可追溯性，不等于测量真实性或独立复核完成。
- 可视化层只消费上游科学结论，不重算单位、QC、置信度或异常方向。
- 自动修复只处理可由证据或规则解决的问题；需要专家判断的进入人工门禁；真实科学限制（如删失值）不计为修复失败。
- loop 最多自动执行两轮；问题不下降或只能靠猜测时返回人工复核。
- high/low 仅是可比背景组内的候选方向，不表述为污染、矿化、矿床或成因。
- 热力图只表达物理采样点密度，不插值元素浓度；三维地球仪与二维图谱同源同口径。
- “产物自包含、断网可用”指交付物形态；数据采集本身在线进行并全程留痕。
- 地图、数据库与报告是 Skill 运行产物；最终提交物仍是一个完整、可复用的 Skill 文档。

## 常见追问

### 这个 Skill 支持哪些平台？

分两层回答。Agent 运行层采用公共 Agent Skills 文件结构，OpenCode 已有隔离评测与 Skill 挂载链路；脚本是普通 CLI，因此 Codex 与本地 CI 可以直接编排。科学数据互操作层提供 EarthChem ECL、USGS AGDB2、ODM2、IGSN/DataCite 的字段映射对照，并已接入十五个公开地球化学来源；映射对照是语义对齐依据，不等于官方认证。

### 为什么不是一个大 Prompt？

语言模型适合解析研究意图、路由来源和组织交付；单位、坐标、Schema、QC、异常阈值、哈希和失败状态属于低自由度任务，必须由确定性脚本执行。九阶段状态机加八个稳定失败状态，让失败也可编程处理，才能跨模型复用并降低运行方差。

### loop 会不会越改越错？

不会直接覆盖标准数据。每个问题有稳定编号、负责人、问题码和证据说明；修复生成新的运行结果并重新验证，历史证据保持不变。最多两轮，无改善、重复阻塞或需要语义判断时立即转人工复核。

### 标准化数据库格式是否自创？

内部采用一行一个“样品 × 分析物 × 测定”的稳定长表，保留原始值与派生值、方法、空间、QC、置信度和来源证据；专业平台字段映射负责交换与审计。稳定内部 Schema 保障一致性和可测试性，字段映射防止与专业生态脱节。

### 异常判定为什么用 robust z？

元素含量分布长尾且各来源方差不同，均值加标准差容易被极值拉偏。我们对 log10 值用中位数与 MAD 计算 robust z，阈值 |z| ≥ 3.5，且背景组严格限定同元素、同介质、同测量基准，最少样本量 20，避免把方法差异误判为地质异常。

### 三维地球仪的数据从哪来？

与二维图谱同一份运行产物：陆地点阵来自内嵌的 Natural Earth 边界，采样点来自 samples.geojson，候选异常点带富集/亏损方向。它是纯 Canvas 自包含单文件，不依赖任何外部库或网络。
